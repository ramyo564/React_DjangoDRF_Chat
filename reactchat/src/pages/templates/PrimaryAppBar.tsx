import { AppBar, Toolbar } from "@mui/material";

const PrimaryAppBar = () => {
    return (
        <AppBar>
            <Toolbar variant="dense">a</Toolbar>
        </AppBar>
    );
};
export default PrimaryAppBar;

